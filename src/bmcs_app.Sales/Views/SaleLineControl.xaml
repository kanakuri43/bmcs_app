<UserControl x:Class="bmcs_app.Sales.Views.SaleLineControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:h="clr-namespace:bmcs_app.Shared.Helpers;assembly=bmcs_app.Shared">

    <Border BorderBrush="{DynamicResource MahApps.Brushes.Gray9}"
            BorderThickness="0,0,0,1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="36"  />  <!-- 行番号 -->
                <ColumnDefinition Width="110" />  <!-- 商品コード -->
                <ColumnDefinition Width="*"   />  <!-- 商品名 -->
                <ColumnDefinition Width="72"  />  <!-- 数量 -->
                <ColumnDefinition Width="88"  />  <!-- 単価 -->
                <ColumnDefinition Width="80"  />  <!-- 原価 -->
                <ColumnDefinition Width="96"  />  <!-- 金額 -->
                <ColumnDefinition Width="80"  />  <!-- 税種別 -->
                <ColumnDefinition Width="56"  />  <!-- 税率 -->
                <ColumnDefinition Width="80"  />  <!-- 税額 -->
                <ColumnDefinition Width="130" />  <!-- 行摘要 -->
                <ColumnDefinition Width="28"  />  <!-- 削除 -->
            </Grid.ColumnDefinitions>

            <!-- 行番号 -->
            <TextBlock Grid.Column="0"
                       Text="{Binding LineNo}"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"
                       Foreground="{DynamicResource MahApps.Brushes.Gray3}" />

            <!-- 商品コード -->
            <TextBox x:Name="ProductCodeBox"
                     Grid.Column="1"
                     Text="{Binding ProductCode, UpdateSourceTrigger=PropertyChanged}"
                     mah:TextBoxHelper.Watermark="コード"
                     h:FocusHelper.MoveNextOnEnter="True"
                     GotKeyboardFocus="OnTextBoxGotKeyboardFocus"
                     Margin="1,1,0,1">
                <TextBox.InputBindings>
                    <KeyBinding Key="Space"  Command="{Binding OpenProductLookupCommand}" />
                    <KeyBinding Key="Return" Command="{Binding LookupProductByCodeCommand}" />
                </TextBox.InputBindings>
            </TextBox>

            <!-- 商品名（表示のみ） -->
            <TextBlock Grid.Column="2"
                       Text="{Binding ProductName}"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource MahApps.Brushes.Gray3}"
                       Padding="4,0"
                       TextTrimming="CharacterEllipsis" />

            <!-- 数量 -->
            <TextBox x:Name="QuantityBox"
                     Grid.Column="3"
                     Text="{Binding Quantity, StringFormat={}{0:0.##}}"
                     TextAlignment="Right"
                     h:FocusHelper.MoveNextOnEnter="True"
                     GotKeyboardFocus="OnTextBoxGotKeyboardFocus"
                     Margin="2,1,0,1" />

            <!-- 単価 -->
            <TextBox Grid.Column="4"
                     Text="{Binding UnitPrice, StringFormat={}{0:N0}}"
                     TextAlignment="Right"
                     h:FocusHelper.MoveNextOnEnter="True"
                     GotKeyboardFocus="OnTextBoxGotKeyboardFocus"
                     Margin="2,1,0,1" />

            <!-- 原価（表示のみ） -->
            <TextBlock Grid.Column="5"
                       Text="{Binding CostPrice, StringFormat={}{0:N0}}"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Right"
                       Foreground="{DynamicResource MahApps.Brushes.Gray3}"
                       Padding="4,0" />

            <!-- 金額（表示のみ） -->
            <TextBlock Grid.Column="6"
                       Text="{Binding LineAmount, StringFormat={}{0:N0}}"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Right"
                       FontWeight="SemiBold"
                       Padding="4,0" />

            <!-- 税種別 ComboBox（RelativeSource で Window.DataContext.TaxTypes を参照） -->
            <ComboBox Grid.Column="7"
                      ItemsSource="{Binding DataContext.TaxTypes,
                                   RelativeSource={RelativeSource AncestorType=Window}}"
                      SelectedItem="{Binding TaxType}"
                      DisplayMemberPath="TaxTypeName"
                      h:FocusHelper.MoveNextOnEnter="True"
                      Margin="2,1,0,1" />

            <!-- 税率（表示のみ） -->
            <TextBlock Grid.Column="8"
                       Text="{Binding AppliedTaxRateDisplay}"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Right"
                       Padding="4,0" />

            <!-- 税額（表示のみ） -->
            <TextBlock Grid.Column="9"
                       Text="{Binding LineTaxAmount, StringFormat={}{0:N0}}"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Right"
                       Padding="4,0" />

            <!-- 行摘要 -->
            <TextBox Grid.Column="10"
                     Text="{Binding LineRemarks, UpdateSourceTrigger=PropertyChanged}"
                     mah:TextBoxHelper.Watermark="（任意）"
                     GotKeyboardFocus="OnTextBoxGotKeyboardFocus"
                     Margin="2,1,0,1">
                <TextBox.InputBindings>
                    <KeyBinding Key="Return" Command="{Binding LineRemarksEnterCommand}" />
                </TextBox.InputBindings>
            </TextBox>

            <!-- 行削除ボタン -->
            <Button Grid.Column="11"
                    Command="{Binding DeleteCommand}"
                    Content="×"
                    IsTabStop="False"
                    FontSize="10"
                    Padding="0"
                    Margin="2,1,1,1"
                    ToolTip="この行を削除"
                    Style="{DynamicResource MahApps.Styles.Button.Flat}" />
        </Grid>
    </Border>
</UserControl>
